apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-app
  template:
    metadata:
      labels:
        app: hello-app
    spec:
      containers:
      - name: hello-app
        image: otaviolxna/hello-app:9c350ec1cbf4
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
